function Resume() {
  return (
    <section className="Resume" aria-labelledby="resume-heading">
      <h2 id="resume-heading">Resume</h2>
      <div className="Resume__lists">
        <ul className="Resume__list">
          <li>Resume</li>
        </ul>
      </div>
    </section>
  );
}

export default Resume;
